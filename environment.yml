name: daynej
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - pytorch
  - sentence-transformers
  - torchaudio
  - torchvision
  - pip:
      - -r requirements.txt
prefix: /opt/miniconda3/envs/daynej
